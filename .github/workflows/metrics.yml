name: Metrics
on:
  schedule: [{cron: "0 */12 * * *"}]
  workflow_dispatch:
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          user: mieszkowskiff
          base: ""
          
          # Konfiguracja wtyczki języków
          plugin_languages: yes
          plugin_languages_ignored: html, css, scss # Możesz tu wpisać języki do ukrycia
          plugin_languages_details: percentage    # Pokazuje procenty
          plugin_languages_sections: most-used    # Skupia się na najczęściej używanych
          plugin_languages_limit: 8